# Spezify the port and the IPv4 for the node
# This will also tell the server where to connect to
NODE_PORT = 1234
NODE_IP = '127.0.0.1'

# Spezify the database user, password
# and databasename
DB_USER = "database_user"
DB_PASS = "database_pass"
DB_NAME = "database_name"